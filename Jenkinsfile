pipeline {
    agent any

    stages {
        stage('Install') {
            steps {
                echo 'Installing dependencies'
            }
        }

        stage('Build') {
            steps {
                echo 'Building application'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests'
            }
        }
    }
}